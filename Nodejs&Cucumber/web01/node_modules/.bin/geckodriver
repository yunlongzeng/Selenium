#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_geckodriver@1.14.1@geckodriver/bin/geckodriver" "$@"
  ret=$?
else 
  node  "$basedir/../_geckodriver@1.14.1@geckodriver/bin/geckodriver" "$@"
  ret=$?
fi
exit $ret
